{
  "inputs": [
    {
      "type": "promptString",
      "id": "supabase_token",
      "description": "Supabase Access Token",
      "password": true
    }
  ],
  "mcpServers": {
    "supabase-bumpbuddy": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase@latest",
        "--access-token",
        "${inputs:supabase_token}"
      ]
    },
    "github": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITHUB_PERSONAL_ACCESS_TOKEN=${env:GITHUB_PERSONAL_ACCESS_TOKEN}",
        "ghcr.io/github/github-mcp-server"
      ]
    }
  }
}
